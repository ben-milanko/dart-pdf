// The native implementation is supplied separately from the web module so
// dart_pdf_editor remains buildable on every Dart target while the Native
// Assets libjpeg-turbo hook is configured.
void installPdfJpegAccelerator() {}
