import 'package:flutter_web_plugins/flutter_web_plugins.dart';

/// Web registration. The public API selects the browser implementation with a
/// conditional import, so no method-channel handler is needed here.
class DartPdfPrintingWeb {
  static void registerWith(Registrar registrar) {}
}
